stages:
  - test
  - build
  - deploy

before_script:
  - export GRADLE_USER_HOME=`pwd`/.gradle

test calc:
  stage: test
  tags:
    - calc
  script:
    - ./gradlew check

build calc:
  stage: build
  cache:
    paths:
      - .gradle/wrapper
      - .gradle/caches
  artifacts:
    paths:
      - build/libs
  tags:
    - calc
  script:
    - ./gradlew assemble

deploy calc:
  stage: deploy
  tags:
    - calc
  script:
<<<<<<< HEAD
    - docker build -t unity --build-arg JAR_FILE=build/libs/*.jar .
    - docker run -p 8080:8080 unity
=======
    - docker build -t htoots/unity --build-arg JAR_FILE=build/libs/*.jar .
    - docker run htoots/unity
>>>>>>> 4fccfb191d0c8ea838f7bff0263f33e24a8f016b
